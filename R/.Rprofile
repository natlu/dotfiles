options(
  rtichoke.color_scheme = "native"
, rtichoke.editing_mode = "vi"
)


library(colorout)


resize <- function(how_wide = Sys.getenv("COLUMNS")) {
  options(width = as.integer(how_wide))
}

# don't save when q()
q <- function(save="no", ...) {
    quit(save=save, ...)
}



options(radian.color_scheme = "monokai")
options(radian.editing_mode = "vi")

options(radian.prompt = "\033[0;36m>\033[0m ")
options(radian.vi_mode_prompt = "\033[36m[{}]\033[0m ")

options(radian.auto_indentation = FALSE)
options(radian.tab_size = 2)
